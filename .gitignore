# Tooling / editor
.freebuff/
.superpowers/
.DS_Store
*.log

# Node
node_modules/

# Expo / React Native
.expo/
dist/
web-build/

# Native build output
android/
ios/
*.apk
*.aab

# Android signing material — never commit a keystore. EAS holds (and can export)
# the one that signs the released APK; losing it means users must uninstall.
*.jks
*.keystore
credentials.json
release.keystore

# Env & secrets
.env
.env.*
!.env.example

# Cloudflare / Wrangler (local dev only — never deploy)
.wrangler/
.dev.vars
